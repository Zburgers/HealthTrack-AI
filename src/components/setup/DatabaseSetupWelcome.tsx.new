/**
 * Database Components Migration
 * 
 * This script provides mappings from the old component structure to the new unified approach.
 * It allows for a gradual transition without breaking existing code.
 */

import DatabaseSetupManager from '@/components/database/DatabaseSetupManager';

// Re-export the new component under the old names for backward compatibility
export { DatabaseSetupManager as default };
