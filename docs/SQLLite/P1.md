✅ PHASE 1 — Requirements Gathering Checklist
1. 🔍 Schema & Data Mapping
You already have your MongoDB schema. Now confirm these:

✅ A complete list of all current MongoDB collections
**LOCAL COLLECTIONS** (stored in local MongoDB):
- `patients` - Patient records with demographics, vitals, symptoms, AI analysis
- `ai_cache` - Cached AI results with expiry (24h default)
- `notes` - SOAP notes and clinical documentation  
- `local_embeddings` - Vector embeddings for local patients
- `db_metadata` - Database initialization metadata

**REMOTE COLLECTIONS** (MongoDB Atlas only):
- `case_embeddings` - MIMIC-IV dataset for vector similarity search

✅ Fields used inside each document (especially dynamic ones like arrays)
**PATIENTS COLLECTION SCHEMA**:
```
_id: ObjectId
name: string
age: number
sex: 'Male' | 'Female' | 'Other' | string
createdAt: Date
last_updated: Date
vitals: {
  temp?: number | null
  bp?: string
  hr?: number | null
  spo2?: number | null
  rr?: number | null
}
symptoms: string[] (ARRAY)
observations: string
primary_complaint?: string
previous_conditions?: string[] (ARRAY)
allergies?: string[] (ARRAY)
current_medications?: string[] (ARRAY)
icd_tags: Array<{
  code: string
  label: string 
  confidence: number
  source_phrase: string
}> (ARRAY OF OBJECTS)
icd_tag_summary: string[] (ARRAY)
risk_predictions: Array<{
  condition: string
  confidence: number
  explanation: string[]
}> (ARRAY OF OBJECTS)
risk_score: number
soap_note: {
  subjective: string
  objective: string
  assessment: string
  plan: string
}
matched_cases: Array<{
  case_id: string
  similarity_score: number
  diagnosis?: string
  summary?: string
  linkType?: 'auto' | 'manual'
  savedAt?: Date
}> (ARRAY OF OBJECTS)
medical_history_analysis?: {
  allergy_warnings?: string[]
  medication_interactions?: string[]
  previous_conditions_impact?: string[]
}
ai_metadata: any
status: 'draft' | 'analyzing' | 'complete' | 'exported' | 'analysis_failed'
owner_uid: string
ai_soap_notes?: string
ai_analysis_timestamp?: Date
isDeleted?: boolean
deletedAt?: Date
deletedBy?: string
deletionReason?: string
```

**AI_CACHE COLLECTION SCHEMA**:
```
_id: ObjectId
key: string (unique cache key)
workflow: string
input: any
output: any
createdAt: Date
expiresAt: Date (TTL index)
```

**NOTES COLLECTION SCHEMA**:
```
_id: ObjectId
patient_id: ObjectId (FK to patients._id)
note_text: string
generated_by_ai: boolean
last_edited: Date
finalized: boolean
```

**LOCAL_EMBEDDINGS COLLECTION SCHEMA**:
```
_id: ObjectId
patient_id: ObjectId (FK to patients._id)
embedding: number[] (vector array)
meta: {
  age: number
  conditions: string[]
  symptoms: string[]
}
createdAt: Date
```

**DB_METADATA COLLECTION SCHEMA**:
```
_id: ObjectId
key: string (unique)
version: string
initializedAt: Date
collections: string[]
lastUpdated: Date
```

✅ Any relations between fields (e.g., patient_id linking to vitals, tags, etc.)
**RELATIONSHIPS**:
- `notes.patient_id` → `patients._id` (FK relationship)
- `local_embeddings.patient_id` → `patients._id` (FK relationship)
- `patients.matched_cases[].case_id` → references remote `case_embeddings._id`
- All collections use `owner_uid` for user isolation
- `ai_cache.key` links to specific patient analysis workflows

✅ Any soft delete logic (isDeleted, deleted_at, etc.)
**SOFT DELETE IMPLEMENTATION**:
- `patients.isDeleted?: boolean`
- `patients.deletedAt?: Date`
- `patients.deletedBy?: string`
- `patients.deletionReason?: string`
**USAGE**: API routes check `isDeleted` field, UI filters show non-deleted records

✅ All enums or constrained values (sex, status, case_type, etc.)
**ENUMS & CONSTRAINTS**:
- `patients.sex`: 'Male' | 'Female' | 'Other' | string
- `patients.status`: 'draft' | 'analyzing' | 'complete' | 'exported' | 'analysis_failed'
- `matched_cases[].linkType`: 'auto' | 'manual'
- NewCaseForm severityLevel: 'Low' | 'Moderate' | 'High' | 'Not Specified'
- NewCaseForm caseType: 'Chronic' | 'Acute' | 'Follow-up' | 'Consultation' | 'Not Specified'
- NewCaseForm gender: 'Male' | 'Female' | 'Other'
- Risk levels: 'low' | 'moderate' | 'high'
- Interaction severity: 'minor' | 'moderate' | 'major'

✅ Confirmation that ObjectIds are not being deeply depended upon
**OBJECTID USAGE**:
- Frontend always converts `_id` to string via `.toString()`
- Patient interface uses `id: string` not ObjectId
- API routes handle both string and ObjectId for compatibility
- No deep ObjectId dependencies - safe for string replacement

✅ Known maximum data volume per patient (for local performance tuning)
**DATA VOLUME ESTIMATES**:
- Patient record: ~5-15KB per patient (with full AI analysis)
- AI cache entry: ~2-10KB per cached analysis
- Notes: ~1-5KB per note
- Local embeddings: ~4KB per embedding (1024 dimensions)
- Expected: 100-1000 patients per clinic = ~1-15MB total
- Cache: ~500 entries = ~5MB
- **TOTAL ESTIMATED**: 10-25MB per clinic

2. 📦 Application Code Analysis
Make a list of all components/services/APIs that interact with MongoDB:

API Routes
✅ /api/patients - GET (list), POST (create)
  - **Operations**: find(), insertOne(), aggregate() for stats
  - **Queries**: owner_uid filter, isDeleted filter, sorting by last_updated
  - **Data access**: Full patient documents, vitals parsing

✅ /api/patients/[id] - GET (read), PATCH (update), DELETE (soft delete)
  - **Operations**: findOne(), updateOne() for SOAP notes/AI analysis
  - **Queries**: _id lookup, soft delete check
  - **Data access**: Full patient documents, nested soap_note updates

✅ /api/enhance-notes, /api/v2/enhance-notes - POST (AI enhancement)
  - **Operations**: Cache lookup/storage via aiCache functions
  - **Queries**: No direct DB queries, uses cache layer
  - **Data access**: Patient data for AI context

✅ /api/similar-cases - POST (vector search)
  - **Operations**: Aggregation pipeline for vector similarity
  - **Queries**: Remote case_embeddings collection
  - **Data access**: Vector search, metadata filtering

✅ /api/case-details - Case information retrieval
✅ /api/export - Data export functionality  
✅ /api/error-reports - Error reporting
✅ /api/health - Health check endpoint

Frontend Pages
✅ /dashboard, /patient/[id], /new-case, /analysis
  - **Dashboard**: Lists patients, uses /api/patients
  - **Patient Detail**: Shows individual patient, uses /api/patients/[id]
  - **New Case**: Creates patients, submits to /api/patients
  - **Analysis**: AI analysis results, updates via /api/patients/[id] PATCH

✅ State management logic that pulls from MongoDB (like Context Providers)
  - **AppStateContext**: Stores analysis results in sessionStorage, not MongoDB
  - **No direct MongoDB queries from frontend** - all via API routes
  - **Local state only** - React state + sessionStorage for UI persistence

Electron-Specific Files
✅ electron/ipc/handlers.ts
  - **Operations**: CRUD operations via IPC handlers
  - **Handlers**: db:getInfo, db:exportData, db:getStorageSettings
  - **Database queries**: listCollections(), countDocuments(), find()
  - **Data access**: Full collection enumeration, document export

✅ electron/db/local-db.ts & electron/local-database.js
  - **Operations**: Database initialization, collection creation, indexing
  - **Queries**: createCollection(), createIndex(), stats()
  - **Data access**: Schema setup, performance optimization

✅ electron/main.ts (DB init logic)
  - **Operations**: Calls initializeLocalDatabase() on startup
  - **Database initialization**: Starts mongodb-memory-server
  - **Data access**: Connection management, error handling

For each, tag how they:

✅ Read/write/delete from MongoDB
**READ OPERATIONS**:
- API routes: find(), findOne() for patient data
- IPC handlers: listCollections(), countDocuments()
- Dashboard: Patient listing with owner_uid filter

**WRITE OPERATIONS**:
- API routes: insertOne() for new patients
- API routes: updateOne() for SOAP notes, AI analysis
- Cache: upsert operations for AI results

**DELETE OPERATIONS**:
- Soft delete: updateOne() sets isDeleted=true
- Cache cleanup: deleteMany() for expired entries
- No hard deletes implemented

✅ Query nested data
**NESTED QUERIES**:
- `patients.vitals.*` field access in API responses
- `patients.soap_note.*` updates in PATCH operations
- `patients.icd_tags[]` array operations
- `patients.matched_cases[]` array manipulation
- No complex nested aggregations currently

✅ Use .aggregate, .distinct, etc.
**AGGREGATION USAGE**:
- Similar cases: Vector similarity aggregation pipeline
- Patient stats: Basic counting aggregations
- No complex multi-stage pipelines
- **DISTINCT**: Not currently used
- **MAP-REDUCE**: Not used

3. ⚙️ Supporting Configuration
✅ Connection Logic: Any .env or config relying on MONGODB_URI
**CONNECTION CONFIGURATION**:
- **Local**: mongodb://localhost:27018/healthtrack_local (mongodb-memory-server)
- **Remote**: MONGODB_URI environment variable for Atlas
- **Config files**: src/lib/mongodb/config.ts, electron/db/local-db.ts
- **Dual database**: Local-primary pattern for patient data

✅ IPC Logic: Look at how DB results are returned via IPC
**IPC COMMUNICATION**:
- **Handler pattern**: electron/ipc/handlers.ts defines handlers
- **Return format**: JSON serialization of MongoDB results
- **Error handling**: Structured error responses with message/details
- **Data transformation**: ObjectId to string conversion for frontend

✅ Local State Sync: Does anything cache MongoDB results in memory/localStorage?
**CACHING LAYERS**:
- **SmartCache**: Multi-tier in-memory + MongoDB cache (src/lib/smartCache.ts)
  - LRU memory cache: 50MB, 1000 entries max
  - Database persistence: 24h TTL
  - Cache warming: Medical scenarios, common workflows
- **API Request Cache**: 5-second TTL for duplicate API calls
- **SessionStorage**: Analysis results, case display data (UI state only)
- **No localStorage**: Patient data never stored in browser storage